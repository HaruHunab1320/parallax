# Parallax Local Runtime Configuration
#
# The local runtime spawns and manages CLI agents on the local machine.
# It provides PTY-based terminal sessions for agent execution.

# Server Configuration
RUNTIME_HOST=0.0.0.0                # Host to bind to
RUNTIME_PORT=9876                   # Port for the runtime HTTP/WebSocket server

# Logging
LOG_LEVEL=info                      # debug, info, warn, error

# Agent Limits
MAX_AGENTS=10                       # Maximum concurrent agents

# Agent Defaults (can be overridden per-agent)
# These are passed to spawned agent processes
ANTHROPIC_API_KEY=                  # API key for Claude-based agents
OPENAI_API_KEY=                     # API key for OpenAI-based agents

# Working Directory
# Agents are spawned in isolated directories under this path
# AGENT_WORK_DIR=/tmp/parallax-agents

# Shell Configuration
# SHELL=/bin/bash                   # Shell to use for PTY sessions
# TERM=xterm-256color               # Terminal type

# Control Plane Connection (for registration)
# PARALLAX_CONTROL_PLANE_URL=http://localhost:3000
