# Parallax Kubernetes Runtime Configuration
#
# The Kubernetes runtime spawns agents as Pods in a Kubernetes cluster.
# Supports both in-cluster and out-of-cluster operation.

# Server Configuration
RUNTIME_HOST=0.0.0.0                # Host to bind to
RUNTIME_PORT=9878                   # Port for the runtime HTTP/WebSocket server

# Logging
LOG_LEVEL=info                      # debug, info, warn, error

# Kubernetes Configuration
PARALLAX_NAMESPACE=parallax-agents  # Namespace for agent pods
# KUBECONFIG=/path/to/kubeconfig    # Optional: kubeconfig path (uses in-cluster config if not set)

# Image Configuration
IMAGE_PREFIX=                       # Optional: registry prefix for agent images
# Example: ghcr.io/myorg/parallax-agents

# Resource Defaults
DEFAULT_CPU=1                       # Default CPU request/limit per agent
DEFAULT_MEMORY=2Gi                  # Default memory request/limit per agent

# Registry (for agent discovery)
PARALLAX_REGISTRY_ENDPOINT=         # Optional: etcd endpoint for registration

# In-cluster detection (auto-detected, do not set manually)
# KUBERNETES_SERVICE_HOST is set automatically when running in a pod

# Agent Secrets
# These should be stored as Kubernetes secrets and mounted to pods
# ANTHROPIC_API_KEY=                # API key for Claude-based agents
# OPENAI_API_KEY=                   # API key for OpenAI-based agents
