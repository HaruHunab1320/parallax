/**
 * SSE Transport
 *
 * Re-exports the SSE transport for remote MCP server access.
 * Note: SSE is deprecated in favor of Streamable HTTP but maintained for compatibility.
 */

export { SSEServerTransport } from '@modelcontextprotocol/sdk/server/sse.js';
