# Parallax Docker Runtime Configuration
#
# The Docker runtime spawns agents as Docker containers.
# Each agent runs in an isolated container with resource limits.

# Server Configuration
RUNTIME_HOST=0.0.0.0                # Host to bind to
RUNTIME_PORT=9877                   # Port for the runtime HTTP/WebSocket server

# Logging
LOG_LEVEL=info                      # debug, info, warn, error

# Docker Configuration
DOCKER_SOCKET=/var/run/docker.sock  # Docker socket path
DOCKER_NETWORK=parallax-agents      # Docker network for agent containers
DOCKER_IMAGE_PREFIX=                # Optional: registry prefix for agent images

# Resource Defaults
DEFAULT_CPU=1                       # Default CPU limit per agent
DEFAULT_MEMORY=2Gi                  # Default memory limit per agent

# Registry (for agent discovery)
PARALLAX_REGISTRY_ENDPOINT=         # Optional: etcd endpoint for registration

# Agent Environment (passed to containers)
ANTHROPIC_API_KEY=                  # API key for Claude-based agents
OPENAI_API_KEY=                     # API key for OpenAI-based agents
