apiVersion: execution.parallax.io/v1alpha1
kind: PatternExecution
metadata:
  name: sentiment-consensus-001
  namespace: default
spec:
  patternRef:
    name: consensus-builder
    namespace: default
  input:
    text: "The new product launch exceeded all expectations with incredible customer feedback!"
    analysis_type: "sentiment"
  parameters:
    detailed_analysis: "true"
  agentSelector:
    capabilities:
      - sentiment
      - text-analysis
  priority: normal
  deadline: "2024-01-20T15:00:00Z"
  successPolicy:
    minConfidence: 0.8
    requiredAgents: 3